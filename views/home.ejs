<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortener</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        header {
            background-color: #333;
            color: white;
            width: 100%;
            padding: 1rem;
            text-align: center;
        }

        main {
            padding: 2rem;
            width: 100%;
            max-width: 600px;
        }

        form {
            display: flex;
            flex-direction: column;
        }

        label {
            margin-bottom: 0.5rem;
        }

        input[type="url"] {
            padding: 0.5rem;
            margin-bottom: 1rem;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        button {
            padding: 0.5rem;
            background-color: #007BFF;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        button:hover {
            background-color: #0056b3;
        }

        .result {
            margin-top: 1rem;
            padding: 1rem;
            border: 1px solid #ccc;
            border-radius: 4px;
            background-color: #f9f9f9;
        }

        .result p {
            margin: 0.5rem 0;
        }
    </style>
</head>
<body>
    <header>
        <h1>URL Shortener</h1>
    </header>
    <main>
        <% if (locals.id) { %>
            <p>shorten url is :  http://localhost:8001/url/<%= id %></p>
         <a href=http://localhost:8001/url/<%= id %>>click me!</a>
         
        <% } else { %>
            <section>
                <form action="/url" method="POST">
                    <label for="longUrl">Enter a long URL to shorten:</label>
                    <input type="text" name="url" required><br>
                    <button type="submit">generate shorten URL</button>
                </form>  
            </section>
        <% } %>
        
    </main>
</body>
</html>
